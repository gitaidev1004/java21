package com.example.app;

public class Car {
    // 필드 (속성)
    String model;
    int speed;

    // 메서드 (기능)
    void drive() {
        System.out.println(model + "이(가) " + speed + "km/h로 주행 중입니다.");
    }
}